export interface HexStackConfig {
  id?: number;
  centerPosition?: [number, number, number];
  width?: number;
  height?: number;
  count?: number;
  colors?: [number, number, number][];
  stackIndex?: number;
}