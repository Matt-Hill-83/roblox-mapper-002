// Super simple test
warn("⚠️ SIMPLE SERVER SCRIPT IS RUNNING!");

const folder = new Instance("Folder");
folder.Name = "SimpleTestFolder";
folder.Parent = game.Workspace;

warn("⚠️ Created SimpleTestFolder in Workspace!");