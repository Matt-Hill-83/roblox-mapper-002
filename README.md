`rojo serve`

# How to Run

1. Install dependencies:

   ```bash
   npm install
   ```

2. Start the Rojo server:

   ```bash
   rojo serve
   ```

3. In a separate terminal, watch for file changes:
   ```bash
   npm run watch
   ```
